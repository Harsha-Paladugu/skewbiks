# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**FORK STATUS:** this repo is **Skewbiks.com**, a Skewb sister site forked from `pyraminx-oo` (the `upstream` remote). Port progress and the remaining milestone plan live in **`docs/port-plan.md`**; machine-verified Skewb domain facts (move tables, state space, symmetries, test vectors, sources) live in **`docs/skewb-ground-truth.md`** — read both before working on engine/renderer/sheet/trainer/solver code. As of 2026-07-13: **M0–M7 are done** (Skewb engine behind the same `window.OOEngine` surface, Skewb renderer, working OO census — Home, oo.html, algs.html, trainer.html and solver.html are fully functional; Firebase project `skewbiks` live with creds in `js/config.js`, rules deployed, and the **census backend is LIVE** — the USER completed the console steps 2026-07-06 (DB + Google sign-in + `admins/{uid}` bootstrap; SETUP.md stays as the reference walkthrough); the sheet pipeline compiles `data/skewb_algs.json` — as of 2026-07-06 the real TCLL / EG2 / NS method sheets, imported from `data/sources/` by `tools/import-method-sheets.mjs`; **the sources' rotation letters are NOT WCA: sheet x/y/z = engine z'/y'/x** — read `data/sources/README.md` before touching them; Sarah's-method subsets still await authoring; the trainer is `src/trainer/skewb-trainer.jsx` + `skewb-core.mjs` — it **fetches `data/skewb_algs.json` at runtime** instead of bundling the sheet, and shares the census's IndexedDB dist table via `js/tables.js`; its recognition centers-quiz resolves answers from the centers the diagram actually shows via `core.quizAnswer` (2026-07-13, pinned in test:trainer): EG2/NS center labels are sig-pure — EG2's direction-less L5C labels fold into the Pi/Peanut vocabulary, its eight directional U-perm labels (U1/U2 × l/f/r/b) condense into a single "U" quiz answer and its numbered pairs (O1/O2, S1/S2, TS1/TS2, W1/W2, X1/X2, Z1/Z2, ZC1/ZC2) fold into their stems (USER decisions 2026-07-13 — 10 EG2 options), NS's lumped "H or Z Perm" rows split into H Perm / Z Perm / Solved, NS's unlabeled L4C/L5C resolve through the same map — while TCLL labels encode twist/pseudo context and stay authored verbatim, and the quiz pins the anchor view (the y² coin-flip made EG2's directional U-perm labels wrong half the time)); the one-look "My solution" preimages are PHYSICAL as of 2026-07-13 (USER-falsified: the engine-frame preimage made solution "U" need a physical L with the layer off-bottom on every draw with a UFL walk digit — `core.preimageOfLayer` now solves `toFixedFacelets(X) = Φ⁻¹(raw Y)` in the facelet model, Y drawn fx[UFL]=0, Φ from `core.physPermOf`; rotation tokens and NS F/R/L/f are rejected at input — those letters move the fixed white/red/green corner and NO rotation-free scramble text can deliver their preimages, machine-verified; pinned in test:trainer against the solver-core physical oracle; drills never had this bug only because the human regrips to the layer-down diagram before running the alg, and the printed-alg-from-raw-case-pattern claim `Φ_alg(raw cs) ∈ SOLVED24` holds identically for the displayed WCA form); **the solver is LIVE as of M7** (2026-07-07): a movecount-only method solver — first-step targets `fl`/`tcll`/`eg2` machine-derived from the imported sheets, and the finishing algorithms are **the sheet texts with their LEADING rotation tokens folded into the one printed setup rotation** (USER revision 2026-07-07: the printed rotation must be exactly what reaches the position the alg's first turn starts at, so the author's own setup rotations are never shown; from the first turn on the text is verbatim, mid-alg rotations included — the finish index and every printed setup rotation are computed in a PHYSICAL facelet model — per text Φ⁻¹ of the 24 solved orientations, junctions matched under 24 rotations of the junction the HUMAN holds, rotation spelled in SHEET letters, every displayed line re-proved by a facelet check; the engine's frame-walk hold reading is physically wrong behind rotation prefixes and is NOT used — machine-verified against three USER-executed junction rotations 2026-07-07, pinned in test:solver); **the whole reconstruction reads in RubiksSkewb notation as of 2026-07-10** — `[lead rotation] [first layer in the sheet vocabulary {R,B,r,b}, never L/l/F/f] [setup rotation] [finish alg]`, the lead rotation chosen so the built layer lands on the bottom (like the sheets/trainer), the layer emitted by `emitNS` (one right-side name per space diagonal), and the whole line physically re-proved end to end in methodView — **derived from the facelets the human actually HOLDS** (`heldFacelets` = physPerm of the scramble TEXT, passed by solver.js; USER-falsified bug 2026-07-10: written free-corner letters (WCA B / NS R L f b) each leave a 240° whole-cube rotation the engine absorbs into its parsing frame, so the pinned state's raw facelets sit rotated in hand and the printed lead was wrong exactly on scrambles with a net written-B twist — counterexample scramble pinned in test:solver; state-only callers default to raw facelets = pre-fix behaviour); fingertrick metrics deliberately deferred (USER will consult top solvers first); its gates are `npm run test:solver` + `tools/solver-lab.mjs`. **Machine-verified caveat:** the engine's `x/y/z` rotation tokens are each the physical INVERSE of the WCA/cubing.js rotation, the sheets' letters differ again, and the engine's frame-walk reading of texts is physically faithful ONLY from identity starts — behind a rotation prefix it diverges from what a human does (ground-truth §Notation notes, machine-verified 2026-07-07 against USER-executed junctions). Engine rotation letters are INTERNAL: humans sight-read SHEET letters, and the solver computes/displays all hold-and-rotation logic in its physical facelet model (js/solver-core.js). **The stored `ns` texts are VERBATIM sheet strings** (machine-established 2026-07-10: 916/928 mid-rotation texts solve their WCA-field case state only under the sheet-letter reading, zero under the engine reading) — display them verbatim, but every MACHINE consumer must read their rotation tokens via `physPermNS` (solver-core), never `physPerm`/engine parse semantics; the engine-letter misread had silently mis-indexed all 916 parseable mid-rotation finish bodies — the other 12 are slash texts that never parse into the index (fixed 2026-07-10 — solver finish coverage turned out essentially COMPLETE: 3109/3110 fl, 11964/11964 tcll, 3204/3204 eg2; the previously documented "~12% gap" was that artifact). The rotationless WCA `alg` field stays the keying/verification form — do NOT convert stored data or one verification surface in isolation. **The Algorithms page shows cases in the community's bat-shaped sheet diagram as of 2026-07-10** (`render.js caseSVG`, geometry lifted pixel-exactly from the Meep/Sarah generator PNGs, D face hidden) drawn from `solver-core.layerDownFacelets(state)` — the raw pinned facelets, rotated so the built layer is on the BOTTOM for the 9 odd-orientation groups — and each alg row displays `sheetLineFor(pic, ns)`: the authored text verbatim whenever it physically proves from the picture (3,073 of 3,116 rows), a re-derived lead otherwise (9 rows; 34 slash texts stay authored) — algs.html loads solver-core.js for this, and the sweep is pinned in test:solver. **The OO census counts hold-24 entries as of 2026-07-10** (machine-verified): the fold is all 24 PROPER rotations — the 12 tetrad-swapping "re-holds" act on states via the re-anchoring map ι (`E.makeHoldSym`, ONE applyParsed pass over the ρ0-conjugated free-corner token stream of a solving word; ground-truth §Symmetry) — giving **132,315 entries** where a position and its LR mirror stay SEPARATE (re-holds preserve handedness, mirrors flip it; solutions are righty-tuned), paired into **66,321 side-by-side pages** by the 48-group `pairId` (`makeFull48Canon`); cache key `oo-classes-v4`, per-side solution caps, admin "Recompute solved bitmap" is the post-deploy migration. **The census has a CF browse scope as of 2026-07-13**: `E.centersRelSolved` flags entries whose six centers are solved relative to each other (12 reachable arrangements = the tetrad-preserving rotations' center configs built from face perms — NOT `sym.apply(solved)`, which is conjugation and returns solved; hold-24-orbit- and mirror-invariant so testing the rep classifies the entry, machine-verified) — **4,503 of the 132,315 entries** ("CF", the theorized-optimal centers-first human method; USER feature 2026-07-13), scope tabs on Browse by depth (`#/browse/cf/<depth>`), CF chip on position pages, home-page link; counts + orbit invariance pinned in `npm run test:space`, fast checks in test:engine (ground-truth §CF subset). Flipping the convention engine-wide is a pending user decision — note the flip target should likely be the SHEET convention, not WCA (users think in sheet letters; x/z differ between the two). `npm run build` is fully green — run `npm run check:fresh` before every commit. The port strategy: the Skewb engine implements the **same `window.OOEngine` member surface** so engine-abstracted consumers (`oo.js`, `tables.js`, `solver.js` UI, `tools/lib/bfs-dist.mjs`) port nearly unchanged. Internal names (`OOEngine`, `OO_CONFIG`, `oo.html`, `oo-*`) are deliberately NOT renamed, to keep upstream cherry-picks clean (`git fetch upstream` + cherry-pick for shared-layer fixes; the repo pins `core.autocrlf=false` — don't let a CRLF checkout poison the hashes). Everything below this paragraph describes the codebase as inherited; where a milestone has landed, the engine/renderer specifics are Skewb (see the two docs).

Skewbiks.com (like the upstream Pyraminx.net) is a **static, no-build-required website**. Five HTML pages share one engine and a set of UI layers. The only build step compiles the algorithm data and bundles the React trainer; everything else runs as plain `<script>` tags in the browser.

## Commands

```bash
npm install
npm run build         # full pipeline: build:trainer (sheet + trainer bundle), then stamp asset hashes, then npm run check
npm run build:trainer # build:sheet, then esbuild-bundle the trainer (node build.mjs) → js/trainer.js
npm run build:sheet   # compile data/skewb_algs.json → js/sheet.js + data/classmap.json
npm run watch:trainer # esbuild watch on src/trainer (node build.mjs --watch) — does NOT recompile the sheet
npm run stamp         # rewrite each asset's ?v= query to its content hash (tools/stamp-assets.mjs)
npm run check         # verify the shipped js/sheet.js against the engine (alias: npm test)
npm run check:fresh   # re-run the pipeline; fail if js/sheet.js, js/trainer.js or any HTML stamp is stale
npm run test:engine   # engine unit tests (tools/test-engine.mjs)
npm run test:trainer  # trainer substrate tests (tools/test-trainer.mjs) — builds TWO full BFS tables, slow-ish
npm run test:rules    # Firestore rules emulator tests (opt-in: needs the emulator + dev deps)

node tools/compile-sheet.mjs --check   # dry-run compile + report; never writes js/sheet.js
```

The esbuild step lives in `build.mjs` at the repo root (entry `src/trainer/index.jsx` → `js/trainer.js`); there is no separate `build:trainer`-only esbuild script — `node build.mjs` is it.

- There is **no unit-test framework**, but three dependency-light test runners exist. `npm test` is aliased to `tools/check-sheet.mjs` (validates the compiled data sheet only). `npm run test:engine` (`tools/test-engine.mjs`) asserts engine invariants — alg parse/notation, mirror/inverse symmetry, `realCanonKey` stability, the optimal BFS solver. `npm run test:trainer` (`tools/test-trainer.mjs`) asserts the trainer substrate (`src/trainer/skewb-core.mjs`) — case model over the JSON, presentation synthesis, masked scrambles, the first-layer predicate/goal tables. `npm run test:rules` (`test/firestore.rules.test.mjs`) is opt-in and needs the Firebase emulator + dev deps. To exercise the engine ad-hoc in Node, replicate what the tools do: `globalThis.window = {}; require('./js/engine.js'); const E = globalThis.window.OOEngine;` (see the "module strategy" gotcha below).
- **To preview the site locally you must serve over HTTP** (e.g. `npx serve` or `python -m http.server`), not `file://` — `js/account.js` uses dynamic `import()` and the OO/Algorithms pages `fetch` the JSON, both of which need an origin.

## Generated files — do not hand-edit

- **`js/sheet.js`** and **`data/classmap.json`** are generated by `tools/compile-sheet.mjs` from `data/skewb_algs.json`.
- **`js/trainer.js`** is the minified esbuild bundle of `src/trainer/` (the source of truth is `src/trainer/skewb-trainer.jsx` + `skewb-core.mjs`; `js/trainer.js` is build output, even though it's the file an editor may surface).

Both are committed so the site works on the host without a build. Edit the *sources*, then rebuild.

## Cache-busting discipline

Every local `js/`/`css/`/`img` asset is loaded with a `?v=<hash>` query in the HTML, where `<hash>` is the first 8 hex of the asset's content sha1. **`npm run stamp` (`tools/stamp-assets.mjs`) rewrites these from the file bytes and is part of `npm run build`** — editing an asset and rebuilding updates every page that loads it automatically; there is no manual `?v=N` to bump. `npm run check:fresh` (`tools/check-fresh.mjs`) re-runs the whole pipeline and fails if the committed `js/sheet.js`, `js/trainer.js`, or any HTML stamp is stale — wire it into CI / run it before committing. (Note: `?v=` queries embedded in CSS `url()`s, e.g. `img/logo.svg` in `css/site.css`, are not stamped — only HTML refs are.)

## Architecture

### Data flow (single authoring source)

```
data/skewb_algs.json   ← authored algorithm list (the authority; v1 = imported method sheets)
   │ npm run build:sheet                          ↖ fetched at runtime by algs.html AND trainer.html
   ▼
js/sheet.js  (SHEET = { ALG / NAME / CNAME / PRES })  +  data/classmap.json   ← generated data-quality gates; no page consumes them at runtime
```

The **Algorithms page, the trainer AND the solver read `skewb_algs.json` directly** (fetched at runtime — the compiled SHEET lacks the `ns`/`rating`/`firstMove`/`nav` fields they display; the solver's finishes are the authored `ns` texts with their leading rotations folded into the printed setup rotation). The compiled `js/sheet.js` + `data/classmap.json` currently have **no page consumer** — they remain the sheet pipeline's machine-checked data-quality gate (`npm run check`). `js/trainer.js` is the esbuild bundle of `src/trainer/` and no longer embeds the sheet. Display notation is normalized everywhere by the shared `engine.normAlg` so all surfaces show identical algs. Sheet keying: renderKey = `stateKey` (the full state — nothing is tip-fixable on a Skewb), canon = `realCanonKey` (y²-fold). There is no state-level 90° `y` symmetry (it swaps the corner tetrads), so a case's four viewing presentations pair at the **data level** via the JSON `direction` field, with the operational convention `prependAUF(p, frontAlg)` = alg for p = 0 Front / 1 Right / 2 Back / 3 Left ("case on the Right → rotate y → run the Front alg"); Front/Back share one canon and Right/Left the other (`caseStateOf` of a y²-prefixed alg equals the y²-sym image — engine-tested).

### The engine is the geometric core

`js/engine.js` (`window.OOEngine`) models the full no-tips Pyraminx state space (933,120 states): state model, moves, A4 rotations + LR mirror, canonicalization, the BFS-table optimal solver, alg parsing/notation, and the **keying + alg→case helpers** (`stateKey`, `realCanonKey`, `caseStateOf`, `algSolvesKey`, `normAlg`, …). State is `{ e:[piece,flip]×6, c:[L,R,B] twists, u: U twist }`. Render keys are edges + U-twist (`stateKey|uTwist`); center twists are tip-fixable and excluded from keys. The other layers are `render.js` (SVG net/3D diagrams, `window.OORender`), `account.js` (Firebase auth + per-user data, `window.OOAccount`), `navbar.js`, `config.js`, and `tables.js` (`window.OOTables`) — the shared IndexedDB cache + BFS dist/class-table builder that **both** `oo.js` (census) and `solver.js` use, with separate cache keys (`oo-dist-v1` for the shared distance table, `oo-classes-v4` for the census's canonical-class tables — v4, USER decision 2026-07-10 (corrected same day, machine-verified): the census folds all 24 PROPER rotations — the 12 tetrad-swapping re-holds act via the re-anchoring map ι, `E.makeHoldSym`/`makeHold24Canon` — into **132,315 entries** that count mirrors SEPARATELY (righty-tuned solutions; each mirror side gets its own ordinal/done-bit/solutions/per-side cap), while `E.makeFull48Canon` (66,321) is the page-grouping `pairId`/Firestore query key with the two sides shown side-by-side on one page; an admin "Recompute solved bitmap" is required after deploy since older doneMaps index the old enumerations) so the two value shapes can't collide. Load order on those pages is `engine.js` → `tables.js` → page script.

The keying helpers are genuinely shared now: the **Algorithms page and the trainer alias them off `window.OOEngine`** (`stateKey`, `realCanonKey`, `caseStateOf`, `prependAUF`, `enumFreeSlots`, `idx`/`unidx`) rather than carrying copies — so a change to move geometry or canonicalization in engine.js reaches every surface. trainer.html loads `js/engine.js` → `js/render.js` → `js/tables.js` before the bundle, so all three `window.*` layers exist at trainer eval; the trainer's dist table IS the census's (`OOTables.loadOrBuildDist`, IndexedDB key `oo-dist-v1`), and its lazy first-layer goal table is cached under the trainer-owned `trainer-fldist-v1`. What legitimately stays local to the trainer (`src/trainer/skewb-core.mjs` — plain ESM so `tools/test-trainer.mjs` can import it in Node) is the masked-scramble generator, the first-layer predicate/goal seeds, and the case model over the JSON — that's substrate, not keying.

### The sheet compiler (hermetic; Skewb re-key landed in M5)

`tools/compile-sheet.mjs` compiles the JSON through engine helpers only and writes two outputs: the `SHEET` data line of `js/sheet.js` (the file is read as a write-template to preserve its surrounding code) and `data/classmap.json` (canonical case key → lowercased subset id, generated from subset membership — never hand-maintain it). The carry-forward mechanism survives from upstream but starts empty: baseline = **committed snapshot `data/prior-sheet.json`** (currently `{}` — the JSON is the sole authority; re-baseline by overwriting it with a freshly built sheet if the JSON ever stops reproducing shipped cases you want kept), and the broken-alg allowlist `data/broken-algs.json` (currently `[]`; an array of `{renderKey, algorithm, reason}`, read by both the compiler and `check-sheet.mjs`). The compiler self-checks every emitted alg and refuses to write outputs that fail; a non-solving alg that isn't in the manifest fails both tools (`UNEXPECTED BROKEN`/`BROKEN`), a *new* unparseable alg fails the build, and an alg authored under a different case than it geometrically solves is an `ADVISORY`. `tools/check-sheet.mjs` re-validates the shipped sheet but shares the engine's keying functions, so it is not independent of engine-level keying bugs.

### Module strategy (why no `"type": "module"`)

The `js/*.js` files are **classic browser scripts** that attach to `window` (`OOEngine`, `OOSheet`, `OORender`, `OOAccount`). The Node tools (`.mjs`, ESM) load them for their **side effect**, not their return value: they set `globalThis.window = {}`, `require()` the file, then read `globalThis.window.OOEngine`. Each `js/` file wraps its body in an IIFE with a local `const module = {exports:{}}` that shadows Node's real `module`. Adding `"type": "module"` to package.json would break the tools' `require()` calls — it is intentionally omitted.

### Auth, roles, and the OO census

`account.js` exposes one shared sign-in session site-wide (Firebase Auth persists per-origin), with a `localStorage` "demo mode" fallback when `config.js` has no Firebase config. `config.js` `adminEmails` gates admin/moderator **UI only** — actual write authorization for the OO census (approving solutions, the public done-bitmap, the `moderators`/`moderatorInvites` collections) is enforced by **Firestore security rules**, version-controlled in `firestore.rules` (wired by `firebase.json`, deploy with `firebase deploy --only firestore:rules`). Admin in the rules is driven **solely** by an `admins/{uid}` collection — `adminEmails` is *not* trusted by the rules at all. These two notions of "admin" are decoupled, which is the main gotcha: an email in `adminEmails` makes the client render the Moderation tab, but if that signed-in account has no `admins/{uid}` document the rules deny every query the tab fires (surfaces as "Missing or insufficient permissions"). Bootstrap the first admin by creating `admins/{your-uid}` in the Firebase console (console writes bypass the rules); your uid is shown on the OO **About** page when signed in. See `SETUP.md` for the full setup/bootstrap walkthrough. Admin edits on the Algorithms page are a per-browser localStorage draft until **Export JSON** → commit → rebuild; there is no live shared store.
